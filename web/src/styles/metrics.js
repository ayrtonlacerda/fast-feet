const SCREEN_HEIGHT = window.innerHeight;
const SCREEN_WIDTH = window.innerWidth;

const PAGE_HEIGHT= SCREEN_HEIGHT - 65;
const MAIN = SCREEN_WIDTH * 0.8 - 50;

export const metrics = {
  SCREEN_HEIGHT,
  SCREEN_WIDTH,
  PAGE_HEIGHT,
  MAIN
};